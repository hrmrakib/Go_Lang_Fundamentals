package main

import "fmt"

func main() {
	var name string = "Rakibul"
	fmt.Printf("What is the type of name variable: %T \n", name)

	var isVerified bool = true
	fmt.Printf("What is the type of isVerified variable: %T \n", isVerified)

	var age int = 25
	fmt.Println(age)
}
